# v4.3.0 GPT-6 migration — active baseline, 2026-09-24

Latest user instruction: continue implementation and acceptance preparation, then
STOP BEFORE final Stable publication for user acceptance. Do not create/publish
the v4.3.0 Release or move the default branch into a published-ready state before
that acceptance. Existing implementation, branch/commit/push and candidate-install
authorizations remain active.

Authorized: implementation, codex/ branch, commits/push/tag, and immutable Stable
Release after required acceptance; transactional exact-candidate installation into
managed CODEX_HOME and three Skills. Preserve user Coordinator settings, unmanaged
Global instructions, old releases, historical evidence and strict ownership/rollback.

Active contract: gpt-6-sol and gpt-6-luna, five efforts; four dynamic Sol views and
one Luna Daily. Central model definitions; generation/axis/policy-bound caches in a
separate namespace. Never use GPT-5.6 Daily/LKG as automatic GPT-6 fallback. Match
publication index/full identity; incomplete cost evidence means quality-only.

Coordinator prepares while Desktop roles are GPT-5.6. Then follow the installed
Skill once for useful direct GPT-6 Sol/Luna tasks and review model/effort/parentage
and overlap. Required: focused/full tests, fresh/rc1/v4.2 lifecycle, preservation,
zero-write repeat/conflict, exact rollback, exact-commit Windows/Ubuntu/macOS CI,
immutable release/tag/default-branch/installed identity. Distinguish source,
fake-home, installed, native, tool visibility, invocation guard and concurrency.
If host reload is unavailable, stop native/release gates and record a precise resume
point; continue independent work. Never infer native PASS from configuration.

2026-09-24 recovery: user reports ModelDial connectivity restored. Preserve the earlier failed Daily/cache evidence. Complete the installed Skill's explicit recovery workflow once, then useful native GPT-6 acceptance; publication remains held for user acceptance.

Initial live evidence: clean b00b769; installed rc1/527b174, ten GPT-5.6 roles and
LKG_FALLBACK_ACTIVE. Desktop runtime 0.155.0-alpha.16; PATH CLI 0.146.0. Radar API
has five GPT-6 rows per family; the matching archive returns 404. Historical
acceptance below remains historical; current results will be recorded separately.

---

# v4.2 Local Coordinator / Sol / Luna Integration

## Authorized v4.2.0 Stable and repository rename — 2026-09-19

The user now explicitly requests the latest version as a formal release and the
previously agreed project rename. This supersedes the earlier prerelease-only
publication scope. Target: Codex Native Workers v4.2.0, repository
SuperDaddyV/codex-native-workers. Preserve installed paths, markers, Skill names,
strict versioned inventories and all immutable historical tags/releases. Verify
old GitHub/raw installation links after the repository rename; update canonical
current entry points, upgrade discovery, Git remote and public documentation.

Stable describes the tested native configuration/routing/transactional installation
product, with user-selected Coordinator and bounded Sol/Luna workers. It does not
add a host-enforced isolation product claim. Historical tool-visibility FAIL stays
FAIL; nested invocation, six-worker capacity and measured savings remain unverified.
No integrity check may be weakened to promote the release. Required checks include
version ordering and rc1-to-Stable lifecycle/preservation, source and three-platform
CI for the exact release commit, immutable release/tag identity, and current
published installation/upgrade instructions. Separate reused native evidence from
new source/fake-home/installed results. Existing rc1 remains immutable.

The rename readback passed: all three previous raw installation contracts matched
their Git blobs exactly; both repository aliases retained repository, rc1 tag,
Release and validation asset identities. Five current public documents passed
GitHub Markdown rendering. Initial local full regression found two stale
rc1-specific documentation/User-Agent expectations; update those expectations to
the Stable product while preserving their behavioral/integrity assertions. Final
exact-source CI and isolated CLI installation evidence belong to the Release asset.
This publication task does not itself apply Stable to the existing real rc1 home.

The completed documentation follow-up commit cbd39d903309e3d68c707ec865bf490e39ca3448
is included in this release work. It passed 431 local tests and three-platform CI
run 35439171006; its branch was published with byte-identical Git objects through
the GitHub API after Git HTTPS transport failed. It has not yet become master;
the new release publication replaces that pending documentation-only promotion.

## Public documentation follow-up — 2026-09-19

The user requested updated GitHub content that explains the project accurately
and helps users install it successfully. Scope: bilingual README and installation
help, version/channel clarity, feedback forms, documentation checks and GitHub
publication of those documentation changes. The published rc1 tag/Release and
runtime source 527b174df13643a38bfe29652208eaa00f63fbf7 remain immutable. Preserve
the v4.1.4 Stable anchors and default Stable status. This follow-up does not
change runtime payloads or reinstall the user's already accepted rc1 installation.
Do not claim an installation success-rate improvement without measured data.

Local follow-up validation: 431 standard-library tests passed (104.638 seconds),
including 24 documentation checks. The exact published rc1 source passed an
isolated Windows CLI installation with two explicit custom roots containing
spaces, dry-run no-write, repeat-apply idempotence and read-only Healthy status
(10 agents, 3 Skills, Daily state still uninitialized). No real-home reinstall or
new native runtime check was performed. All three pinned raw installation
contracts matched their Git source, and GitHub's Markdown API rendered both
READMEs and both help guides with tables, fenced prompts and intact Unicode.
Publication and exact-commit CI are subsequent checks, not implied by this record.

## Authorized Native Workers preview delivery — 2026-09-19

The user approved completing the proposed scope change end to end without another
approval round. This includes repository implementation, tests, bounded native
delegation, transactional installation of the reviewed release, Git commits/push,
and publication of a disclosed prerelease. It does not authorize unrelated
configuration changes, credential handling, destructive history edits, or Stable
promotion. Existing unpublished work is part of this delivery and must be kept.

Target: **Codex Native Workers v4.2.0-rc1**. Keep the repository URL, managed paths,
Skill names, and historical immutable release anchors compatible. Keep the daily
selection algorithm unchanged. Add an explicit worthwhile-delegation trigger;
the Global managed block must remain at most 2,048 UTF-8 bytes including markers.

Acceptance for this preview separates product correctness from host capabilities:

- Required: safe installation/upgrade/rollback, strict inventory/ownership checks,
  correct selection and model/effort, useful bounded Sol and Luna execution,
  Coordinator review, disjoint mixed work, source regression and release identity.
- Host capabilities: tool visibility, invocation enforcement and maximum observed
  concurrency have their own evidence/status. Keep the historical leaf FAIL;
  strong prevention of recursive delegation is unsupported until independently
  verified. Continue requesting agents.enabled=false and prohibit worker spawning.
- No six-worker, strict isolation, comparative cost or quota claim without the
  corresponding evidence. A known tool-visibility failure does not block unrelated
  checks under this newly approved, narrower preview contract. Unexpected worker
  delegation, wrong model/effort, ownership drift or unsafe writes still stop the
  affected acceptance immediately. Never convert an old FAIL into a PASS.
- Diagnostic schema 4 must distinguish leaf configuration from unmeasured native
  delegation/tool isolation/invocation enforcement; support historical readers
  and manifests without weakening their integrity checks.

Plan: implement policy and diagnostics in disjoint bounded tasks; integrate
branding, version and release documentation; run focused/full checks and native
acceptance; pin a reviewed candidate commit and transactionally install it for
native acceptance before publication. This candidate apply is authorized by the
current end-to-end implementation request, separately from published-release
discovery. Publish that exact commit only after source CI and installed checks;
verify the published Release/tag against the installed source. Record post-commit
acceptance in the sanitized Release evidence, without moving the tag or claiming
an untested source. The prior Stable remains the default Stable target.

Build-time checkpoint: 430 standard-library tests passed; the three Skills
passed structural validation, and the managed Global block is 2,044 bytes. The
Coordinator reviewed disjoint work from two Sol/max and one Luna/max workers
with observed overlap and verified role metadata. Source CI, candidate install,
post-install native checks and publication had not completed at this checkpoint.
Their exact-commit results belong in the sanitized Release evidence. Consult the
published v4.2.0-rc1 Release before inferring that these steps are still pending;
this immutable source checkpoint is not the final delivery receipt.

Fresh pre-change evidence on 2026-09-19: Desktop host 0.155.0-alpha.9.2 loaded new
Sol/max and Luna/max children with useful outputs. Sol reported six collaboration
tools including spawn_agent/followup_task; Luna's tool report remained unknown.
Neither invoked delegation. Config/source ownership passed. This does not close
the old leaf gate or establish invocation enforcement; issue 45066 remains open.

## Upstream report submitted — 2026-09-13

The user explicitly authorized external submission. The sanitized report was
created as [openai/codex issue 45066](https://github.com/openai/codex/issues/45066).
A separate read verified its exact title/body and open state. No private paths,
session identifiers, credentials or raw session attachments were submitted.
Earlier unsubmitted-draft statements below describe their historical phases.
Native leaf remains FAIL, exact root cause BLOCKED, and downstream capacity
checks NOT RUN. Submission does not establish a maintainer-confirmed defect or
close acceptance. No runtime configuration, Git commit or Release was changed.

## Authorized documentation repair — 2026-09-12

The user approved repairing the confirmed repository-documentation regression
in the current implementation task. The separate native acceptance task remains
the place for independent verification after a targeted runtime fix exists.

The public installation record now uses a portable backup-path placeholder.
Exact recovery information and the original documents remain in ignored local
evidence; the recorded transaction backup still exists. No tests or integrity
criteria were changed. No installed runtime, Global configuration, selection,
branding, Git history or publication was changed by this repair.

Validation: all four repository-safety tests PASS; the full standard-library
suite passes **423 tests in 101.355 seconds**. Final evidence-text edits are
covered by another focused repository check and diff validation before delivery.
Earlier failed test runs remain historical evidence below. This repair does not
resolve native leaf FAIL, the blocked root-cause diagnosis, or unrun six-worker
capacity checks; the upstream report remains unsubmitted.

## Diagnostic closeout — upstream draft prepared, not submitted

The remaining advertised local entry points were inspected through help/schema
only. server/diagnostics provides process memory and numeric gauges, not child
effective settings or native tool definitions. The agents command is a session
browser; doctor advertises auth inspection and no child-specific mode, so it was
not executed. No new native check, credential access or external upload occurred.

RUNTIME_TESTS.md now contains a sanitized upstream report draft with observed
setup, reproduction steps, restart evidence, expected behavior and explicit
limits of child-reported tool visibility. It requests effective setting/layer,
emitted tool registry and invocation-guard evidence from the host maintainers.
The draft is not submitted. Native leaf FAIL and downstream BLOCKED/NOT RUN
remain unchanged. Repeating the same local checks has no new evidence target;
resume root-cause diagnosis when host traces or relevant implementation evidence
become available. Only these two evidence documents changed in this follow-up;
diff validation PASS, previous full-suite failure remains unresolved.

## Post-restart leaf verification — 2026-09-12 — FAIL persists

The user restarted Codex Desktop and authorized verification. The observed host
process started at 23:44:52 BJT; its binary still reports 0.154.0-alpha.6.2.
Installed owned files match source/manifest and three Skill ownership hashes
match. This new post-restart workflow ran the installed delegation selector once,
returning today's schema-2/reference-policy-1 sol_max/general and luna_max profile.

Two NEW native children were created, rather than resuming the old children.
Parent-visible metadata confirms Sol/max and Luna/max, multi_agent_version=v2,
direct depth-1 parentage; a parent snapshot shows both running concurrently.
Both returned useful scoped checks and their completion sentinels.

**FAIL:** fresh Sol reports all six collaboration tools, including spawn_agent
and followup_task; fresh Luna reports those two tools as visible as well. Neither
invoked delegation. Thus restarting the host and creating fresh children did not
remove the reported tool exposure. This does not prove nested invocation would
succeed, nor does it isolate all possible persistent-cache or precedence issues.
The complete leaf gate remains failed. **NOT RUN / BLOCKED:** six-worker capacity
and downstream acceptance, stopped at the first substantive failure as required.

Only these evidence documents were edited; no reinstall, global changes or Git
mutation. Documentation diff check PASS. Source suite not rerun for repeated
native evidence; its previously recorded one-failure result remains unresolved.

## Leaf diagnosis follow-up — read-only host inspection

The user authorized continuing diagnosis after the failed native acceptance.
The observed Desktop host and both child session headers identify
`0.154.0-alpha.6.2`; both child turn contexts report `multi_agent_version=v2`.
The PATH CLI is separately `0.146.0` and is not this runtime. Both children also
received the custom role's no-delegation developer instruction, so complete
failure to load the role file is excluded; individual setting precedence is not.

Current official configuration/subagent documentation supports `agents.enabled`
and custom-agent configuration layers. No evidence supports renaming the key or
reinstalling the unchanged payload. The absence gate remains FAIL; successful
nested execution remains NOT RUN, not proven possible or prohibited.

The exact host binary's exported experimental app-server schema provides no
per-child effective-config/tool-registry inspection method. `config/read` accepts
cwd/includeLayers, not a thread ID; `thread/read` exposes metadata/history, not
the effective agents setting or full native tool registry. No generic native
tools/list request is advertised. Consequently the available read-only evidence
cannot distinguish override precedence, omitted filtering, or advertised tools
with separate invocation enforcement. Root-cause identification is BLOCKED by
this observability gap; the diagnosis does not establish a specific host bug.

Needed host evidence: for the same child creation/continuation, report the final
agents.enabled value and layer origin, the native tool registry sent to the model,
and whether a separate invocation guard exists. Inspect these before proposing
any repair. No new agents, nested probes, daemon starts, global writes, credential
reads, Git operations or implementation changes were made. The exported schema
is an ignored local diagnostic artifact. See RUNTIME_TESTS.md for references.

## Resumed native acceptance — 2026-09-12 — FAIL (leaf tool surface)

The user explicitly resumed the interrupted attempt below. Current fingerprints
confirmed no installed/source/state drift; only the two authorized evidence
documents differed from the original baseline. The Beijing date remained the
same. Both original children resumed with the same roles and the workflow reused
its single installed selection; no additional selection or child was created.

- **FAIL — native leaf:** both Sol and Luna report actual exposure of
  `collaboration.spawn_agent` and `collaboration.followup_task`. Sol additionally
  reports send_message, interrupt_agent, list_agents and wait_agent. These are
  returned runtime tool-surface observations, not inferred from TOML. No child
  exercised delegation; successful nested spawning was deliberately not tested.
  The protocol requires tool absence, so exposure fails that gate even though
  every installed role contains `agents.enabled=false`.
- **PASS — useful native outputs:** Sol verified Coordinator/Worker boundaries
  and exact template/installed managed-policy equality; Luna verified ten role
  mappings and ten source/installed SHA-256 matches, with no mismatches. Both
  returned their required completion sentinels. Parent-visible resumed turn
  metadata confirms `gpt-5.6-sol/max` and `gpt-5.6-luna/max`; the earlier direct
  parentage and two-family overlap evidence remains applicable.
- **PASS — separate read-only status check:** the installed status Skill command
  ran once, returned schema 3, Healthy/OK, agents 10/10, Skills 3/3, leaf Ready,
  configured capacity 6, native runtime/capacity Not checked. Protected file and
  four Daily/LKG JSON hashes remained unchanged. Healthy/Ready is configuration
  evidence and does not override the native leaf FAIL.
- **BLOCKED — overall mixed acceptance and downstream capacity acceptance:** the
  first substantive failure stopped downstream native tests. **NOT RUN:** six
  concurrent independent Workers, overflow rejection/queuing, small-task routing,
  permitted Sol adjustment and equivalent-task/cost comparison. Maximum actual
  capacity remains unverified; observed concurrency remains at least two.

Minimum follow-up boundary: diagnose the host's native custom-agent tool filtering
for `agents.enabled=false`, distinguishing tool advertisement from invocation
enforcement. No configuration workaround, nested probe, reinstall, implementation
repair or publication is authorized by these results. Preserve the failed gate;
do not redefine leaf as merely not using exposed delegation tools.

This continuation changes only PLANS.md and RUNTIME_TESTS.md. Source/fake-home
validation is recorded separately in the detailed resumed runtime record.

Delivery verification: `git diff --check` PASS. The required full suite ran 423
tests in 97.110 seconds and **FAIL**ed one existing repository-safety check:
the historical installation record below contains an absolute private user
backup path. That path was present at this task's initial read, before either
evidence edit. It is preserved rather than silently rewriting historical
evidence. No checks were skipped or weakened; the historical 423-PASS record is
not current acceptance. This source-test failure is separate from native leaf.

## Fresh-task native acceptance attempt — 2026-09-12 — BLOCKED (interrupted)

The user authorized read-only native children and recording actual results in
this file and `RUNTIME_TESTS.md`, without reinstall, global configuration changes
or Git publication. The attempt was interrupted; both spawned children are now
`interrupted`, with no returned useful output or tool-surface report.

- **PASS:** current local.2/schema-3 ownership; twelve source-identical installed
  files; three source-rendered Skills; both managed blocks; ten discovered roles
  with correctly pinned models/efforts and leaf configuration.
- **PASS:** exactly one installed Worker selection, current Beijing date, schema
  2/reference policy 1; `sol_max` general and `luna_max`, both public benchmark
  `cloudflare-reference/custom_endpoint`, `reference_only`, no fallback or
  capability degradation. Sol selection remains quality-only.
- **PASS (bounded evidence):** native metadata confirms Sol/max and Luna/max,
  direct depth-1 parentage, and a parent snapshot shows both running together.
  Current Coordinator metadata is Astra/medium, not historical Astra/high.
- **BLOCKED:** useful-output acceptance, native leaf tool absence and completed
  mixed-task acceptance, because execution was interrupted before results.
- **NOT RUN:** six-worker overlap, beyond-cap rejection/queuing, small-task
  routing, permitted Sol adjustment, status execution and comparative benchmark.
  Seven advertised total slots/configured six Workers are not measured capacity;
  observed concurrency is at least two, with the maximum unverified. No usage or
  savings claim is made. Source/fake-home suite was not rerun for this attempt.
- **FAIL:** no substantive failure observed; interruption alone does not justify
  a repair. Before documentation writes, all 97 captured repository/installed
  fingerprints were unchanged, including Global rules/config and Daily/LKG state.

See the detailed attempt in `RUNTIME_TESTS.md`. These results supersede the older
blanket NOT RUN statement only for the checks actually observed; full native
acceptance remains incomplete. No reinstall or global/Git mutation occurred.

## Approved reference-data adaptation and local installation — 2026-09-12

The user approved the proposed follow-up: implement explicit public reference
benchmark support, validate live selection, and transactionally install this
reviewed local candidate into the real existing Codex and Skill roots. This
authorization supersedes the earlier repository-only/no-real-install boundary
for this operation. It does not authorize GitHub publication, Git mutations,
credential access, unrelated configuration repair, or changing the Coordinator.

- Keep the unreleased candidate version `v4.2.0-local.2`; add a versioned reference
  selection policy to distinguish earlier daily caches.
- Accept only the explicit benchmark source pairs `codex/official_login` and
  `cloudflare-reference/custom_endpoint`. Preserve the actual source label;
  public benchmark evidence is `reference_only`, never local runtime proof.
- Compare one coherent source pair per family, capability and batch. Keep each
  result evidence ID, while protocol/batch identity establishes comparability.
- Resolve complete backend data through the publication index for the API's
  exact batch. Require matching protocol/hash/count/pricing metadata. Missing
  artifacts or costs retain valid API quality selection; no older-cost mixing.
- Preserve the legacy Luna-only CLI and algorithm. The Worker entry point owns
  the explicit reference policy and independent family caches.
- After focused/full tests and current public API acceptance, dry-run and apply
  through the existing installer with both roots explicit. Preserve non-managed
  Global rules, user model/effort and legacy daily/LKG records. Keep the backup.
- Installed ownership/status and fresh native execution are separate gates.
  If this already-running task cannot discover the new Sol roles, provide a
  concrete fresh-task acceptance prompt rather than pretending they ran.

### Reference adaptation and real local installation acceptance

- Final offline suite: **423 tests PASS**, 106.078 seconds; all three Skills pass
  the UTF-8 Skill validator, and `git diff --check` passes. This replaces the
  earlier 407-test source baseline for the current candidate.
- Fresh public acquisition and the installed Worker selector both return live
  `luna_max`; Sol general `sol_max`, backend `sol_xhigh`, frontend `sol_high`,
  reasoning `sol_max`. Both families retain
  `cloudflare-reference/custom_endpoint`, `evidence_scope=reference_only`.
- The publication index points to a missing same-batch archive (HTTP 404 in the
  inspected publication). Current acquisition reports
  `unavailable_or_mismatched`; all four Sol views use `quality_only`. The source
  identity rejection is resolved; complete-cost optimization is not established.
- Final real dry-run: PASS, seven new and five modified items, no removals.
  Transactional apply at 2026-09-12 22:59 BJT: **UPGRADED**, local.1/schema 2 to
  local.2/schema 3. The reviewed dirty source is not labelled as an immutable
  Git commit. No Git commit, push, tag or Release was created.
- Installed acceptance: all twelve owned files match source bytes and manifest
  hashes; all three rendered Skills match ownership hashes. Global non-managed
  bytes and configured Astra/high are unchanged. Legacy Daily/LKG hashes are
  unchanged after the new Worker profile initialization. The managed Global
  block is 1,882 bytes including its terminating newline, below 2 KiB.
- One installed read-only status result reports `Healthy`, `OK`, agents 10/10,
  Skills 3/3, leaf configuration `Ready`; native runtime and runtime capacity
  remain `Not checked`. Configured worker capacity is six, not observed capacity.
- Transaction backup is retained at
  `<CODEX_HOME>/backups/sol-luna-v4/<backup-name>`.
  The exact original path is retained in ignored
  `.var/reference-install-result.json`; local diagnostic evidence is under
  ignored `.var/reference-*` files. This public path was redacted by the approved
  documentation repair; the underlying transaction evidence was not rewritten.
- Fresh Sol execution, mixed-family native delegation, six simultaneous Workers
  and comparative task-cost acceptance remain NOT RUN. This running task still
  exposes its previous Luna roles and four total agent slots. Use the fresh-task
  handoff in `RUNTIME_TESTS.md`; installed status is not native-runtime proof.

## Earlier repository-only baseline — 2026-09-12

The user approved integrating the unpublished local repairs with the following
repository-only implementation. This section supersedes conflicting scope limits
in the historical local.1 plan below; historical results are not new acceptance.

- Candidate: `v4.2.0-local.2`. User-selected Astra is the Coordinator; the software
  retains a model-agnostic Coordinator boundary and never sets the parent model.
- Add five fixed-model, fixed-effort Sol leaf profiles alongside five Luna leaves.
  Preserve Luna's backend winner/tie algorithm and legacy selection CLI.
- A single daily Worker entry point shares public data acquisition, selects Sol
  general/backend/frontend/reasoning views with a versioned two-point quality
  band and comparable complete-cost minimization, and isolates each family's
  validation, availability, and LKG. Missing comparable cost means quality-only.
- Pin a daily selection for a workflow; do not refresh separately for each child.
  Unknown schemas, mismatched batch links, malformed scores and absent evidence
  cannot be promoted to a valid current recommendation.
- Concurrency target: usually zero to three, up to six independent ready direct
  Workers, bounded by actual host capacity. No hard Sol share, no nested workers.
- Global managed policy budget: 2 KiB including markers. Keep essential authority,
  explicit delegation trigger, minimal receipt and three Skill entry points.
  Put details in `sol-luna-delegate`; preserve status/upgrade read-only/authorization
  boundaries. Keep formulas and data validation in code, not the global prompt.
- Manifest schema 3 owns ten agents, three Skills and the shared selector module.
  Preserve strict historical schema-2 two-Skill compatibility, transactional
  rollback prevalidation, user root ownership, daily/LKG state and unrelated files.
- Verify targeted regressions, full offline suite, Skill validation, byte budget,
  isolated lifecycle/upgrade/rollback, diff and protected real-file fingerprints.
  Native Sol model/leaf/mixed delegation/six-child capacity remain separate gates;
  source or fake-home success must never claim native-runtime acceptance.
- No real CODEX_HOME installation/repair, credential access, commit, branch, tag,
  push, PR or GitHub Release. Existing dirty changes remain part of this candidate.

Implementation ownership: Coordinator integrates legacy CLI/cache/status/smoke and
documentation; bounded Workers own pure Sol selection, installer lifecycle, and
policy/Skill/agent payloads respectively. Final acceptance belongs to Coordinator.

### Local.2 acceptance — 2026-09-12

- Final full offline gate: `python -m unittest discover -s tests -q` — **407 tests
  PASS**, 107.463 seconds on the current Windows host. An earlier integration run
  failed; old fixture anchors/byte hashing and schema-3 manual rollback validation
  were corrected, with internal partial-write recovery preserved, before this
  complete rerun. No failing tests were skipped or weakened to obtain this result.
- Lifecycle coverage: 52 tests, including schema-2/Stable migration, exact schema-3
  ownership, corrupted rollback rejection, and failure after the Skill phase.
- Installed-layout fixture acceptance: the copied selector imports its sibling
  module, returns a valid Sol-only profile when Luna evidence is absent, preserves
  legacy state, and exposes schema-3 status without writing state.
- Three Skills pass the Skill creator validator using Python UTF-8 mode. Five
  bounded forward scenarios checked routing, runtime ceilings, partial views,
  status errors and leaf behavior; these are instruction checks, not native proof.
- Rendered Global managed block: **1,882 UTF-8 bytes including markers**, within
  the 2,048-byte cap. Discovery metadata still has a context cost; no quota or
  whole-task savings claim is made from this byte measurement.
- Protected real policy, config, installed selector, manifest and legacy daily/LKG
  fingerprints remain unchanged relative to the captured execution baseline.
- Source diff check passes. Existing unpublished work remains in the candidate;
  no commit, branch, tag, push, PR, Release or real installation was performed.

Local source/fake-home acceptance is complete. Live recommendation readiness is
blocked by the source identity limitation below. New Sol native model/effort,
leaf tools, mixed delegation, six-child capacity and comparative task efficiency
remain **NOT RUN**. They require suitable source evidence, a separately authorized
installation, and a fresh task that loads the new profiles. The current task did
not require another conversation to finish repository implementation.

### Current public-data limitation

The 2026-09-12 live check received API schema 1.1 (generated
`2026-09-12T09:46:52.323Z`) and the independently dated full snapshot
(`2026-09-11T12:25:09.529Z`). Both Sol and Luna currently use the explicitly
reported `cloudflare-reference/custom_endpoint` identity in this data. The
strict `codex/official_login` adapters therefore reject those rows. A clean
repository test state returned both families unavailable, as intended; no real
installed state was used as a substitute or changed for this check.

This blocks fresh live recommendations and downstream mixed native acceptance.
It does not mean Codex lacks either model. Do not relabel routes, fabricate a
current profile, or install the candidate to conceal this limitation. A future
decision must either obtain suitable source evidence or separately review an
explicit reference-route policy, its labels and comparability constraints.

## Historical local.1 baseline and evidence

## Goal

Build and validate a local-only `v4.2.0-local.1` candidate that reduces the
always-loaded Global policy, moves low-frequency status and upgrade procedures
into Skills, and stops treating the coordinator model name `Sol` as a permanent
architecture boundary.

## Scope

- Keep `Coordinator` as the model-agnostic root role and `Worker` as the bounded
  execution role.
- Preserve `Sol/Luna` only as the current product, command, selector-role, and
  receipt compatibility vocabulary.
- Add `sol-luna-status` and `sol-luna-upgrade` Skill payloads.
- Install Skill payloads only through an explicit, transaction-owned Skill root.
- Compress the Global policy without weakening delegation, acceptance, receipt,
  or fail-closed requirements.
- Validate source behavior and fake-home lifecycle locally.

## Constraints

- Do not commit, push, tag, create a Release, or modify GitHub.
- Do not install into or repair the real Global `CODEX_HOME` or user Skill root.
- Preserve unrelated user files, configuration, Skills, and dirty-worktree state.
- Keep source, fake-home, fresh-task runtime, and real Global evidence separate.
- Do not change the Daily Luna selector algorithm, ModelDial ranking input, Luna
  model, allowed efforts, concurrency limit, or native-leaf behavior.

## Work plan

1. Add and validate the two focused Skill payloads.
2. Refactor repository and Global policies to the Coordinator/Worker abstraction.
3. Render the status command into the status Skill and replace detailed Global
   status/upgrade sections with fail-closed Skill routing.
4. Extend the installer manifest and transaction so the two Skill files have
   explicit ownership, backup, rollback, idempotency, conflict, and uninstall
   coverage without taking ownership of unrelated Skills.
5. Add byte-budget and semantic regression tests for the compact Global policy.
6. Run focused tests, the full unit suite, and a fake-home lifecycle; do not run
   a real installation or claim fresh-task runtime acceptance.

## Acceptance criteria

- The managed Global policy is at most 4.5 KiB and contains no detailed status or
  release-discovery procedure.
- Both Skills pass the bundled Skill validator and focused semantic tests.
- The Global policy and Luna agent prompts use `Coordinator` for architectural
  authority; `Sol` is not defined as the permanent root model.
- Installer dry-run is write-free; apply is idempotent; conflicts fail closed;
  rollback and uninstall restore both roots exactly in fake-home tests.
- The full local unit suite passes, with any unrun runtime or publication gates
  reported explicitly.

## Out of scope

- Selecting or naming the next OpenAI coordinator model.
- Publishing `v4.2.0`, changing public Stable documentation, or updating immutable
  release anchors.
- Real Global apply, current ownership-mismatch repair, or fresh-task acceptance.

## Local verification — 2026-09-04

- Source candidate: `v4.2.0-local.1`; publication and real Global apply remain
  out of scope.
- The rendered managed policy for the current standard Global path is `4,413`
  bytes. Read-only projection against the current Global file reduces its
  managed block from `9,328` to `4,413` bytes (`52.7%`) and total file size from
  `15,897` to `10,982` bytes (`30.9%`) without modifying that file.
- Both Skill payloads pass the bundled Skill validator.
- `python -m unittest discover -s tests -q`: `376` tests passed.
- An isolated CLI fake-home cycle passed dry-run, apply, second-apply
  idempotency, schema-2 status (`Skills 2/2 Ready`), uninstall, exact rollback,
  and final cleanup of both fake roots.

## Local acceptance repairs — 2026-09-07

The joint local acceptance found five issues despite the existing 376-test
suite passing. The user authorized repository-local repairs; real installation,
global configuration changes, Git operations, and publication remain excluded.

1. Validate the complete rollback backup before changing either target root.
2. Reject incomplete schema-2 Skill ownership and mismatched Skill roots.
3. Preserve pre-existing empty Skill roots and their parent directories.
4. Reject contradictory schema-2 Skill health in compatibility smoke.
5. Preserve reader health when today's selection is not initialized.

Add reproducing regressions first, run focused checks after each repair, then
run the full suite and inspect the revision diff and protected-file hashes.
The real installation will retain the pre-repair payload. Native-leaf runtime
tool visibility was not verified by the acceptance and is not a source repair.

### Completed repair verification

- Rollback now stages and validates every restore entry and payload before
  writing or deleting in either root. Corrupted/missing later payloads and
  malformed/duplicate entries fail without changing the installed tree;
  legacy single-root backups remain supported.
- Schema-2 manifests require the exact two-Skill hash inventory and retain
  directory creation ownership through idempotent installs and upgrades.
  Unknown ownership in older schema-2 manifests preserves empty roots.
- Smoke rejects contradictory or malformed Skill inventories across healthy,
  benign degraded, and project override states. Status instructions preserve
  installation errors independently of today's selection initialization.
- Reproducing regressions failed before the relevant repairs. The final full
  suite passed `387` tests, with no skips, in `83.726` seconds on local Windows.
  It includes `48` installer lifecycle and `29` compatibility smoke tests.
- Both Skill payload validators and `git diff --check` passed. The repair scope
  is ten repository files; pre-existing unrelated changes were preserved.
- Real protected files and selector state retained their pre-repair hashes.
  The installed status Skill therefore still contains the older text; no
  installer apply, global repair, authentication copy, commit, push, or Release
  was performed. Source/fake-home results do not close the native-leaf runtime
  evidence gap or constitute post-repair joint runtime acceptance.
