# GPT-6 synthetic regression fixtures

Derived from the preserved ../modeldial test fixtures by changing exact model
identities. Canonical caches also bind the GPT-6 model/axis/policy contract.
These are artificial test inputs, not measured GPT-6 benchmarks or runtime evidence.
Live ModelDial data and native evidence are recorded separately.
